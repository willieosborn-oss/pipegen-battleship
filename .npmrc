# Fail fast with a clear "unsupported engine" error on an out-of-range Node,
# instead of the cryptic Rolldown native-binding MODULE_NOT_FOUND that Node
# < 20.19 produces (the "silent bindings" finding from manual play).
engine-strict=true
